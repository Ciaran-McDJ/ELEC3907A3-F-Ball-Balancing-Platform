# File to hold personal functions I want to use
import math


def cosd(deg:float):
    """Take cosine of angle in degrees"""
    return math.cos(math.radians(deg))

def sind(deg:float):
    """Take sin of angle in degrees"""
    return math.sin(math.radians(deg))